/** Mirrors MachineReading — what <machine-stats> shows, one meter and one value each. */
export enum MachineReading {
  Cpu      = 'cpu',
  Memory   = 'memory',
  Swap     = 'swap',
  Received = 'received',
  Sent     = 'sent',
  Load     = 'load',
}
