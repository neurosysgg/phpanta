/** Mirrors MachineAttribute — what the machine service's elements read off the page. */
export enum MachineAttribute {
  Source  = 'data-source',
  Reading = 'data-reading',
  Entry   = 'data-entry',
}
