# A checkout of Phpanta on its own: composer and npm install here, and the suites write caches.
/vendor/
/node_modules/
/build/
.phpunit.cache/
# The test app's data/: LoginRecipeTest mints a session key there for its own run, and removes it.
/test/fixture/app/data/
/composer.lock
